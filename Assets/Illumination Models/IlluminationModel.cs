using UnityEngine;

namespace Illumination_Models
{
    public class IlluminationModel : MonoBehaviour
    {
        
        // Flat, gouraud, and phong
        // Flat：The polygon's reflection is calculated by its vector alone
        // Gouraud：the polygon's reflection color is calculated as the linear interpolation of the color of the vertices of the triangle
        // Phong: The polygon's reflection is calculated by the per-pixel basic of the linear interpolation of the three vertices of the polygon
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
